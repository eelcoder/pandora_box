version: '1.0.{build}'

image: Visual Studio 2017

platform:
- x86
- x64

configuration:
- Debug
- Release

install:
  - mkdir build && cd build
  - cmake .. -DCMAKE_BUILD_TYPE=%CONFIGURATION%

build:
   project: c:/projects/pandora_box/build/ALL_BUILD.sln
   parallel: true
   verbosity: minimal

test_script:
- ctest -j2

deploy: off